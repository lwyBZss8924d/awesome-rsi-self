Operate the Awesome RSI Self Daily Dispatcher for one bounded run.
Use the configured source repository and local daily config supplied by this automation.
Invoke only kb dispatch --config LOCAL_CONFIG --apply --json using the automation's
actual configured source repository and local config path. The deterministic dispatcher
prioritizes exactly one safe pending-publication run with its saved original config;
it never creates a replacement research run while that publication is unresolved.
Unknown/running/ambiguous run states require owner reconciliation. Never remove a live
lock or restart a running phase. If no run is pending, the dispatcher requires a clean
configured base branch and fast-forwards it before starting one finite fresh run.
If action=restart_required, source synchronization changed HEAD and no daily worker
has started. End that CLI process. Invoke the same kb dispatch command and run ID in a
fresh process loaded from restart.expected_revision; do not reuse cached API bindings or
hooks. Keep the reported budget_deadline_at/remaining budget for this scheduled cycle.
If that budget is exhausted, retain the receipt and defer to the next scheduled
invocation. Do not report restart_required as a completed knowledge run.
Wait for the final durable result. Do not manually reproduce worker steps or launch a
second pipeline. Report action, source_before, source_after, source_scan_complete,
checks_passed, pr_open, merged and
projection_synced separately, with the exact run/checkpoint path and next action.
A process exit or valid JSON is not a completed research/publication result. Missing
counts are unavailable. Do not claim native Scheduled acceptance from a manually run CLI.
Preserve the task's actual target_thread_id and physical Codex home.
